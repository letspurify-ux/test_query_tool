# SPACE Query

Rust + FLTK 기반 Oracle Query Tool입니다.

## 실행

```bash
./run.sh
```

## 빌드 (`SPACE Query` 실행파일 생성)

```bash
./build_space_query.sh
```

릴리즈 빌드:

```bash
./build_space_query.sh --release
```
